package ontrack;

public class TaskSubmissionValidator {
    
    public ValidationResult validate(TaskSubmission submission) {
        ValidationResult result = new ValidationResult();
        // No validation logic yet - test will fail
        return result;
    }
}